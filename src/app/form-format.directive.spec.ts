import { FormFormatDirective } from './form-format.directive';

describe('FormFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFormatDirective();
    expect(directive).toBeTruthy();
  });
});
